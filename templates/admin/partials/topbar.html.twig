<div class="topbar">
    <!-- Reszta zawartości topbaru -->
    <div class="user-info">
        <div class="toggle-dropdown">
            <img class="avatar" src="{{ asset('build/images/brand/logo.png') }}" alt="user avatar" height="20" width="20">
            <span class="username" id="username">{{ app.user.email }}</span>
        </div>
        <ul class="dropdown-menu">
            <li><a href="javascript:void(0);">Settings</a></li>
            <li><a href="{{ path('dev_logout') }}">Logout</a></li>
        </ul>
    </div>
</div>
